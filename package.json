{
  "name": "client-blog-server",
  "version": "0.0.1",
  "description": "Client blog application json server to emulate network requests",
  "main": "api/server.js",
  "scripts": {
    "start": "node api/server.js"
  },
  "dependencies": {
    "json-server": "^0.17.0"
  },
  "devDependencies": {
    "prettier": "^3.3.3"
  }
}
